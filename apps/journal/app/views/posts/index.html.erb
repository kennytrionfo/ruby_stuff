<h1>All Journal Posts</h1>

<table>
  <thead>
    <tr>
      <th>Kid</th>
      <th>Author</th>
      <th>Subject</th>
      <th>Body</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.kid %></td>
        <td><%= post.author %></td>
        <td><%= post.subject %></td>
        <td><%= post.body %></td>
        <td><%= post.date %></td>
        <td><%= link_to 'Show', post %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
